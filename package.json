{
  "name": "azd-exec",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm run test:cli:unit && pnpm run test:cli:integration && pnpm run test:web",
    "test:cli": "pnpm run test:cli:unit && pnpm run test:cli:integration",
    "test:cli:unit": "cd cli && go test -v -short ./src/...",
    "test:cli:integration": "cd cli && go test -v -tags=integration -timeout=10m ./src/...",
    "test:web": "pnpm --filter @jongio/azd-exec-docs test",
    "test:all": "pnpm test"
  },
  "devDependencies": {}
}
