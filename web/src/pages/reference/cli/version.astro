---
import Layout from '../../../components/Layout.astro';
import CodeBlock from '../../../components/CodeBlock.astro';

const base = import.meta.env.BASE_URL;
---

<Layout title="azd exec version" description="Show azd exec version information">
  <div class="page-container">
    <div class="page-header">
      <h1><code>azd exec version</code></h1>
      <p class="page-intro">
        Display version information for the azd exec extension.
      </p>
    </div>

    <div class="content">
      <section class="section">
        <h2>Synopsis</h2>
        <CodeBlock code='azd exec version [flags]' language="bash" />
        <p>
          Shows the current version of the azd exec extension, build information,
          and compatibility details.
        </p>
      </section>

      <section class="section">
        <h2>Flags</h2>
        <div class="flags-table">
          <table>
            <thead>
              <tr>
                <th>Flag</th>
                <th>Description</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><code>--help</code>, <code>-h</code></td>
                <td>Show help for this command</td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>

      <section class="section">
        <h2>Examples</h2>
        
        <h3>Show Version</h3>
        <CodeBlock code='azd exec version' language="bash" />
        
        <h3>Sample Output</h3>
        <CodeBlock code={`azd exec version 0.1.0`} language="text" showCopy={false} />
      </section>

      <section class="section">
        <h2>Version Information</h2>
        <p>The version command displays:</p>
        <ul>
          <li><strong>Version number</strong>: Semantic version (e.g., 0.1.0)</li>
          <li><strong>Build date</strong>: When the extension was built (in development builds)</li>
          <li><strong>Commit hash</strong>: Git commit identifier (in development builds)</li>
        </ul>
      </section>

      <section class="section">
        <h2>Checking for Updates</h2>
        <p>To check if a newer version is available:</p>
        
        <h3>Windows (PowerShell)</h3>
        <CodeBlock code={`# Show installed version
azd exec version

# Check latest release on GitHub
\$latest = (Invoke-WebRequest -Uri "https://api.github.com/repos/jongio/azd-exec/releases/latest" | ConvertFrom-Json).tag_name
Write-Host "Latest version: \$latest"`} language="powershell" />

        <h3>macOS/Linux (Bash)</h3>
        <CodeBlock code={`# Show installed version
azd exec version

# Check latest release on GitHub
latest=\$(curl -s https://api.github.com/repos/jongio/azd-exec/releases/latest | grep '"tag_name"' | cut -d'"' -f4)
echo "Latest version: \$latest"`} language="bash" />
      </section>

      <section class="section">
        <h2>Updating azd exec</h2>
        <p>To update to the latest version:</p>
        
        <h3>Windows (PowerShell)</h3>
        <CodeBlock code='azd extension install jongio/azd-exec --force' language="powershell" />

        <h3>macOS/Linux (Bash)</h3>
        <CodeBlock code='azd extension install jongio/azd-exec --force' language="bash" />
      </section>

      <section class="section">
        <h2>Version History</h2>
        <p>
          For a complete changelog of all versions, see the 
          <a href="https://github.com/jongio/azd-exec/blob/main/cli/CHANGELOG.md" target="_blank" rel="noopener noreferrer">CHANGELOG.md</a>
          on GitHub.
        </p>
      </section>

      <section class="section">
        <h2>Related Commands</h2>
        <div class="related-commands">
          <a href={`${base}reference/cli/`}>CLI Overview</a>
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .page-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
  }

  .page-header {
    margin-bottom: 3rem;
  }

  .page-header h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin: 0 0 1rem;
    color: var(--color-text);
  }

  .page-header h1 code {
    font-family: var(--font-mono);
    font-size: 2rem;
  }

  .page-intro {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    margin: 0;
  }

  .content {
    line-height: 1.7;
  }

  .section {
    margin-bottom: 3rem;
  }

  .section h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin: 0 0 1rem;
    color: var(--color-text);
  }

  .section h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 2rem 0 1rem;
    color: var(--color-text);
  }

  .section p {
    margin: 0 0 1rem;
    color: var(--color-text-muted);
  }

  .section ul {
    margin: 0 0 1rem;
    padding-left: 1.5rem;
  }

  .section li {
    margin: 0.5rem 0;
    color: var(--color-text-muted);
  }

  .flags-table {
    margin: 1.5rem 0;
    overflow-x: auto;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9375rem;
  }

  thead {
    background-color: var(--color-surface-2);
  }

  th {
    text-align: left;
    padding: 0.75rem 1rem;
    font-weight: 600;
    color: var(--color-text);
    border-bottom: 2px solid var(--color-border);
  }

  td {
    padding: 0.75rem 1rem;
    color: var(--color-text-muted);
    border-bottom: 1px solid var(--color-border);
  }

  td code {
    background-color: var(--color-surface-2);
    padding: 0.125rem 0.375rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
  }

  .related-commands {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .related-commands a {
    color: var(--color-primary);
    font-weight: 600;
    text-decoration: none;
  }

  .related-commands a:hover {
    text-decoration: underline;
  }
</style>
